# Tool configuration only. This repository is a learning journal, not a package.
[tool.ruff]
line-length = 79            # PEP 8
target-version = "py310"

[tool.ruff.lint]
select = ["E", "W", "F"]    # pycodestyle errors/warnings and pyflakes
